import ReservationsList from "../components/ReservationsList";

const UserReservedExperiences = () => {
  return (
    <div>
      <ReservationsList />
    </div>
  );
};

export default UserReservedExperiences;
